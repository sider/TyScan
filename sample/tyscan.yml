rules:
  - id: sample
    message: Do not use console.log
    pattern: console.log
    tests:
      match:
        - console.log("abc")
        - console.log( # Broken test
      unmatch:
        - console.error("abc");
